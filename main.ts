/**
 * @author Nicholas Sun
 * @version 1.0.0
 * @date 2025-11-18
 * @fileoverview This program calculates the area of a circle.
 */

// Declaring variables
var radius: number = 5.6;
const PI: number = 3.14;

var area: number = PI * radius * radius;

// Printing
console.log("A circle with radius of " + String(radius) + " cm has an area of " + String(area) + " cm^2.");